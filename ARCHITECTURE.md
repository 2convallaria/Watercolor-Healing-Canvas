# 水彩疗愈画板 — 项目技术架构说明文档

> 面向对象：面试官 / 技术评审
> 项目状态：首个可运行 MVP 已完成
> 技术形态：纯前端单文件 Web 应用（HTML + CSS + Canvas 2D，零依赖）

---

## 1. 项目概述

水彩疗愈画板是一款以「指尖触碰画布，时间绽放水彩」为核心理念的解压型绘画 Web 应用。用户按住画布时，水彩晕染随按压时长逐渐扩散（10秒封顶160px），松手后以不规则水彩滴形态留存；配合柔和色系、水彩边缘沉积质感与柔光混色，营造疗愈、放松的创作体验。

技术选型采用 **原生 JavaScript + Canvas 2D + 单文件 HTML** 架构，无需任何框架与依赖，一次点击即可在任意现代浏览器（桌面端 / 移动端）运行。

## 2. 总体架构

### 2.1 分层架构

| 层级 | 核心职责 | 关键实现 |
| :--- | :--- | :--- |
| **UI 表现层** | 提供用户交互界面 | 顶部工具栏、预设调色板、自定义颜色、清空按钮 |
| **交互控制层** | 统一处理多端输入事件 | Pointer Events、单指针状态机、防多指冲突 |
| **业务逻辑层** | 处理核心数据与规则 | 按压时长映射、形状生成、颜色空间转换 |
| **渲染引擎层** | 负责画布绘制 | Canvas 2D、径向渐变、混合模式、局部重绘 |
| **性能调度层** | 保障流畅度与稳定性 | setTimeout 节流、rAF、定时器回收、DPR 适配 |

### 2.2 模块划分

| 模块 | 职责 |
|------|------|
| 调色板模块 | 春日花海 / 静谧海洋 / 落日余晖三套预设 + 自定义 Hex 颜色 |
| 颜色工具模块 | Hex ↔ RGB 转换、低饱和高明度柔和色系约束 |
| 形状生成模块 | 随机半径扰动 + 贝塞尔曲线闭合，生成不规则水彩滴 |
| 渲染模块 | createRadialGradient 水彩晕染、混合模式、边缘沉积 |
| 交互模块 | pointerdown/move/up/cancel 状态机、多指防冲突 |
| 性能调度模块 | 100ms 节流绘制循环、松手清理定时器、局部脏区重绘 |

## 3. 核心技术与实现要点

### 3.1 交互模型：Pointer Events 统一事件源
- 采用 Pointer Events 统一处理鼠标 / 触摸 / 触控笔，避免 mousedown + touchstart 双通道重复触发。
- 单指针状态机：仅追踪首个 pointerId，第二根手指直接忽略；pointercancel 兜底固化，防止异常中断导致状态残留。
- CSS `touch-action: none` + 禁用缩放 / 回弹，保证移动端拖动时不会触发页面滚动与手势冲突。

### 3.2 按压时长 → 晕染半径（时间驱动生长）
- 半径 = min(按压时长 / 10000ms × 160px, 160px)，采用指数缓动，**10 秒缓慢洇开、封顶 160px**，杜绝无限膨胀。
- 生长过程每 100ms 节流重绘一次，视觉平滑且不消耗额外性能。

### 3.3 不规则水彩滴形状
- 极坐标下对半径做 0.70 ~ 1.30 随机扰动，生成约 10 个控制点。
- 使用二次贝塞尔曲线（quadraticCurveTo）经中点平滑闭合，形成自然、不规则的滴状轮廓。
- 形状种子在按下时固定，生长过程仅整体缩放，保持形态连贯，避免每帧抖动。

### 3.4 水彩晕染：径向渐变 + 边缘沉积
- 使用 `createRadialGradient` 构建三段式透明度曲线，这是「水彩感」的核心。
- 中心极淡（alpha 0.15），过渡区（alpha 0.35），边缘沉积加深（alpha 0.75），模拟真实水彩“颜料随水分堆积在边缘”的物理特征。

### 3.5 双模式与混色策略
- **墨水模式**：采用 `source-over` 正常叠加，配合高频毛刺波形，模拟黑笔漏墨的深邃感。
- **水彩模式**：采用 `soft-light`（柔光）混合模式，避免正片叠底导致颜色发黑变脏；边缘极柔（alpha 0.05），让不同颜色叠加时自然产生梦幻的混色。
- 防脏色约束：全局 `globalAlpha` 控制在 0.8 左右，绘制完成后立即恢复为 1.0，避免状态泄漏污染后续绘制。

### 3.6 性能与稳定性设计
- **绝对禁止** `ctx.filter = 'blur()'`：每帧滤镜会带来灾难性性能开销，移动端直接卡死。
- **禁止全屏实时重绘**：仅对当前水滴包围盒做局部 clearRect + 局部重绘。
- 绘制循环用 setTimeout 固定 100ms 节流，配合 requestAnimationFrame 调度；松手时同时 `clearTimeout` + `cancelAnimationFrame`，杜绝后台空转与定时器泄漏。
- DPR（最高 2x）高清适配；resize / 旋转屏幕时离屏快照等比搬移，已有画作不丢失。

## 4. 关键设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 技术栈 | 原生 JS + Canvas 2D | 零依赖、单文件、移动端兼容，突出对底层 API 的掌控能力 |
| 渲染方式 | Canvas 2D 局部重绘 | 相比 DOM/SVG 拥有像素级渐变与混色控制；相比全屏重绘大幅节省移动端开销 |
| 晕染实现 | createRadialGradient | 一次调用构建连续透明度渐变，性能远优于逐像素计算 |
| 不规则形状 | 极坐标扰动 + 二次贝塞尔 | 可控随机性 + 平滑轮廓，避免规则圆形的呆板 |
| 定时调度 | setTimeout 100ms + rAF | 时间驱动生长且绘制频率受限，兼顾动画连续性与性能 |
| 事件模型 | Pointer Events + 状态机 | 统一多端输入，天然防多指、防重复触发 |

## 5. 性能与防崩溃保障清单
- [x] 无全屏重绘，仅局部脏区更新
- [x] 无 ctx.filter blur 等重滤镜操作
- [x] 绘制循环 100ms 节流，松手即回收定时器
- [x] globalAlpha 使用后恢复 1.0，避免污染后续绘制
- [x] 工具栏点击通过 `closest('#toolbar')` 隔离，不误触画布
- [x] DPR 上限 2x，防止高分屏内存暴涨

## 6. MVP 已实现功能清单
- [x] 双模式切换（墨水模式 / 水彩模式）
- [x] 三套预设调色板（春日花海 / 静谧海洋 / 落日余晖）+ 自定义 Hex 颜色
- [x] 按压时长控制晕染大小，10 秒封顶 160px
- [x] 随机不规则水彩滴形状（贝塞尔曲线）
- [x] 水彩晕染（径向渐变、边缘沉积）
- [x] 柔光混色，色彩通透不脏
- [x] 松手后图形保留，可持续叠加创作
- [x] 桌面端鼠标 + 移动端触摸完整兼容

## 7. 未来迭代方向与产品演进

当前 MVP 验证了“时间驱动 + 水彩混色”的核心机制。接下来计划从“纯自由涂鸦”向“特定场景的疗愈互动”演进，解决涂鸦类应用缺乏长期留存的问题：

### 7.1 场景化疗愈交互（产品差异化）
- **黑白植物复苏**：引入黑白线稿图层（Mask）。用户按压不仅出现水彩，更是“浇水”的动作。按压时间越久，黑白植物获得的水彩越饱满，花朵绽放越盛。通过“养成”和“治愈”提升用户成就感。
- **情绪小兽安抚**：设计蜷缩的灰色小动物。用户用水彩为其染色，在色彩晕染的过程中，小动物舒展身体、发出呼噜声（配合音效），让用户获得情感陪伴。

### 7.2 收集与留存机制（提升 DAU）
- **节气限定卡**：结合二十四节气，推出“立春·粉绿”、“冬至·冷蓝”等专属调色盘。用户只有在特定节气前后才能解锁，提供收集价值。
- **每日挑战**：随机掉落一张空白线稿（如敦煌壁画残片），用户需要用水彩将其修复，生成可分享的海报。

### 7.3 沉浸式感官体验（强化“疗愈”标签）
- **ASMR 音效**：加入按压时的水声、纸张沙沙声，以及松手瞬间的“叮”声。声音与按压时长绑定，提供视觉之外的听觉解压。
- **背景互动**：新增星空、宣纸等背景材质，水彩落在不同材质上会呈现不同的扩散边缘（纸面物理模拟）。

### 7.4 技术架构演进
- 离屏 Canvas / Web Worker 进一步提升绘制性能。
- WebGL 水彩物理模拟（颜料扩散、纸面纹理）。
- 迁移为微信小程序，扩大移动端触达，增加社交分享属性。
