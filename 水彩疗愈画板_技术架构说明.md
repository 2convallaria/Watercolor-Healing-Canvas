---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 287430317185896_0-drive/225637215796020620/水彩疗愈画板_技术架构说明.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 287430317185896#1789552552899
    ReservedCode2: ""
---
# 水彩疗愈画板 — 项目技术架构说明文档

> 面向对象：面试官 / 技术评审
> 项目状态：首个可运行 MVP 已完成，效果达到预期
> 技术形态：纯前端单文件 Web 应用（HTML + CSS + Canvas 2D，零依赖）

---

## 1. 项目概述

水彩疗愈画板是一款以「指尖触碰画布，时间绽放水彩」为核心理念的解压型绘画 Web 应用。用户按住画布时，水彩晕染随按压时长逐渐扩散（5 秒封顶 150px），松手后以不规则水彩滴形态留存；配合柔和色系、水彩边缘沉积质感与正片叠底 / 柔光混色，营造疗愈、放松的创作体验。

技术选型采用 **原生 JavaScript + Canvas 2D + 单文件 HTML** 架构，无需任何框架与依赖，一次点击即可在任意现代浏览器（桌面端 / 移动端）运行，兼顾移动端触摸性能与部署的极简性。

## 2. 总体架构

### 2.1 分层架构

```
┌───────────────────────────────────────────────┐
│ UI 表现层    顶部工具栏（调色板 / 自定义颜色 / 清空）│
├───────────────────────────────────────────────┤
│ 交互控制层   Pointer Events 统一事件 · 单指针状态机│
├───────────────────────────────────────────────┤
│ 业务逻辑层   按压时长映射 · 形状生成 · 颜色选取/转换│
├───────────────────────────────────────────────┤
│ 渲染引擎层   Canvas 2D · 径向渐变 · 混合模式 · 局部重绘│
├───────────────────────────────────────────────┤
│ 性能调度层   setTimeout 节流 · rAF · 定时器回收 · DPR 适配│
└───────────────────────────────────────────────┘
```

### 2.2 模块划分

| 模块 | 职责 |
|------|------|
| 调色板模块 | 春日花海 / 静谧海洋 / 落日余晖三套预设 + 自定义 Hex 颜色 |
| 颜色工具模块 | Hex ↔ RGB 转换、低饱和高明度柔和色系约束 |
| 形状生成模块 | 随机半径扰动 + 贝塞尔曲线闭合，生成不规则水彩滴 |
| 渲染模块 | createRadialGradient 水彩晕染、混合模式、边缘沉积 |
| 交互模块 | pointerdown/move/up/cancel 状态机、多指防冲突 |
| 性能调度模块 | 100ms 节流绘制循环、松手清理定时器、局部脏区重绘 |

## 3. 核心技术与实现要点

### 3.1 交互模型：Pointer Events 统一事件源

- 采用 Pointer Events 统一处理鼠标 / 触摸 / 触控笔，避免 mousedown + touchstart 双通道重复触发。
- 单指针状态机：仅追踪首个 pointerId，第二根手指直接忽略；pointercancel 兜底固化，防止异常中断导致状态残留。
- CSS `touch-action: none` + 禁用缩放 / 回弹，保证移动端拖动时不会触发页面滚动与手势冲突。

### 3.2 按压时长 → 晕染半径（时间驱动生长）

- 半径 = min(按压时长 / 5000ms × 150px, 150px)，线性增长，**5 秒封顶**，杜绝无限膨胀。
- 生长过程每 100ms 节流重绘一次，视觉平滑且不消耗额外性能。

### 3.3 不规则水彩滴形状

- 极坐标下对半径做 0.70 ~ 1.30 随机扰动，生成约 10 个控制点。
- 使用二次贝塞尔曲线（quadraticCurveTo）经中点平滑闭合，形成自然、不规则的滴状轮廓。
- 形状种子在按下时固定，生长过程仅整体缩放，保持形态连贯，避免每帧抖动。

### 3.4 水彩晕染：径向渐变 + 边缘沉积

- 使用 `createRadialGradient` 构建三段式透明度曲线，这是「水彩感」的核心：

| 位置 | 透明度 alpha | 含义 |
|------|------|------|
| 中心（0.0） | 0.02 | 极淡的湿润中心 |
| 过渡区（0.65） | 0.10 | 颜料向边缘流动 |
| 边缘（1.0） | 0.25 | 边缘沉积加深，模拟水彩水痕 |

- 中心透明、边缘留痕，符合真实水彩「颜料随水分堆积在边缘」的物理特征。

### 3.5 混色策略：source-over 为基础 + 正片叠底 / 柔光

- 默认以 source-over（正常叠加）保证色彩通透，在此基础上受控引入正片叠底 / 柔光混合模式，增强多层晕染叠加的层次感。
- 防脏色约束：
  - 颜色一律先由 Hex 转为 RGB，再以 rgba() 写入渐变，杜绝字符串拼接带来的格式隐患。
  - 全局 `globalAlpha` 控制在 0.15 左右，绘制完成后立即恢复为 1.0，避免状态泄漏污染后续绘制。
  - 混合模式受控使用，避免无约束的正片叠底导致颜色发黑变泥。

### 3.6 性能与稳定性设计

- **绝对禁止** `ctx.filter = 'blur()'`：每帧滤镜会带来灾难性性能开销，移动端直接卡死。
- **禁止全屏实时重绘**：仅对当前水滴包围盒做局部 clearRect + 局部重绘。
- 绘制循环用 setTimeout 固定 100ms 节流，配合 requestAnimationFrame 调度；松手时同时 `clearTimeout` + `cancelAnimationFrame`，杜绝后台空转与定时器泄漏。
- DPR（最高 2x）高清适配；resize / 旋转屏幕时离屏快照等比搬移，已有画作不丢失。

## 4. 关键设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 技术栈 | 原生 JS + Canvas 2D | 零依赖、单文件、移动端兼容，突出对底层 API 的掌控能力 |
| 渲染方式 | Canvas 2D 局部重绘 | 相比 DOM/SVG 拥有像素级渐变与混色控制；相比全屏重绘大幅节省移动端开销 |
| 晕染实现 | createRadialGradient | 一次调用构建连续透明度渐变，性能远优于逐像素计算 |
| 不规则形状 | 极坐标扰动 + 二次贝塞尔 | 可控随机性 + 平滑轮廓，避免规则圆形的呆板 |
| 定时调度 | setTimeout 100ms + rAF | 时间驱动生长且绘制频率受限，兼顾动画连续性与性能 |
| 事件模型 | Pointer Events + 状态机 | 统一多端输入，天然防多指、防重复触发 |

## 5. 性能与防崩溃保障清单

- [x] 无全屏重绘，仅局部脏区更新
- [x] 无 ctx.filter blur 等重滤镜操作
- [x] 绘制循环 100ms 节流，松手即回收定时器
- [x] globalAlpha 使用后恢复 1.0，避免污染后续绘制
- [x] 工具栏点击通过 `closest('#toolbar')` 隔离，不误触画布
- [x] DPR 上限 2x，防止高分屏内存暴涨

## 6. MVP 已实现功能清单

- [x] 三套预设调色板（春日花海 / 静谧海洋 / 落日余晖）+ 自定义 Hex 颜色
- [x] 按压时长控制晕染大小，5 秒封顶 150px
- [x] 随机不规则水彩滴形状（贝塞尔曲线）
- [x] 水彩晕染（径向渐变、边缘沉积）
- [x] 正片叠底 / 柔光混色，色彩通透不脏
- [x] 松手后图形保留，可持续叠加创作
- [x] 桌面端鼠标 + 移动端触摸完整兼容

## 7. 技术亮点（面试表达建议）

1. **对 Canvas 底层 API 的深度理解**：用 createRadialGradient 三档透明度曲线模拟水彩边缘沉积，而非简单实心圆。
2. **移动端性能意识**：从架构层面杜绝全屏重绘与重滤镜，通过局部脏区 + 节流调度保证流畅。
3. **事件工程能力**：Pointer Events + 单指针状态机统一多端输入，防重复、防冲突、防误触。
4. **视觉算法**：极坐标随机扰动 + 贝塞尔闭合生成可控不规则形状；颜色空间约束避免混色发脏。

## 8. 未来迭代方向

- 离屏 Canvas / Web Worker 进一步提升绘制性能
- 增加按压音效、水彩流动动画，增强沉浸感
- 颜色混色限制策略（防止过度叠加产生脏色）
- 迁移为微信小程序，扩大移动端触达
- WebGL 水彩物理模拟（颜料扩散、纸面纹理）

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
